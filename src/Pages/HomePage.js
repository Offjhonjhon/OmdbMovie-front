import styled from 'styled-components';

function Home() {
    return (
        <HomePageContainer />
    )
}

export default Home;

const HomePageContainer = styled.div`
    background-color: black;
    width: 100vw;
    height: 100vh;
`